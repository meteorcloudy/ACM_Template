*************************************欧拉函数*************************************
ll euler(ll x)
{   ll k=(ll) sqrt(x);
    ll ans=1;
    for (int i=2;i<=k;i++)
        if (x%i==0)
        {     x/=i;
              ans*=i-1;
              while (x%i==0)
              {     x/=i;
                    ans*=i;
              }
        }
    if (x!=1) ans*=x-1;
    return ans;
}
***************************************************************************************************
